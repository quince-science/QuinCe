# This file contains the run type categories for the application.
# When an instrument is defined, its run types will be assigned
# to one of these categories (or IGNORED, which is handled internally
# by the application).
#
# The configuration contains the following columns:
#
# Code: The code of the category for internal storage
# Name: The human-readable name of the category
# Type: The category type. 0 = measurement, 1 = calibration
# MinCount: The minimum number of run types required in this category
#
#Code,Name,Type,MinCount
MCO2,Marine CO₂,0,1
ACO2,Atmospheric CO₂,0,0
STD,Gas Standard,1,3
