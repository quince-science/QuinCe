package uk.ac.exeter.QuinCe.data.Dataset.QC;

import java.lang.reflect.Type;

import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonParseException;
import com.google.gson.JsonPrimitive;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;

/**
 * JSON serializer and deserializer for QC flags.
 *
 * <p>
 * This class serializes {@link Flag} instances as a simple integer.
 * {@link RoutineFlag} instances are serialized by
 * {@link RoutineFlagSerializer}.
 * </p>
 * <p>
 * This class is used to deserialize JSON generated by both this class and
 * {@link RoutineFlagSerializer} into simple {@link Flag} objects.
 * </p>
 *
 * @see RoutineFlagSerializer
 */
public class FlagSerializer
  implements JsonSerializer<Flag>, JsonDeserializer<Flag> {

  /**
   * The {@link FlagScheme} to use for parsing and translating {@link Flag}s.
   */
  private FlagScheme flagScheme;

  /**
   * Constructor.
   *
   * @param flagScheme
   *          The {@link FlagScheme} to use for parsing and translating
   *          {@link Flag}s.
   */
  public FlagSerializer(FlagScheme flagScheme) {
    this.flagScheme = flagScheme;
  }

  @Override
  public JsonElement serialize(Flag src, Type typeOfSrc,
    JsonSerializationContext context) {

    return new JsonPrimitive(src.getValue());
  }

  @Override
  public Flag deserialize(JsonElement json, Type typeOfT,
    JsonDeserializationContext context) throws JsonParseException {

    Flag result;

    try {
      /*
       * JSON from Flag instances will just be the int flag value. JSON from
       * RoutineFlag instances is an object containing the routine name and the
       * flag value. For both cases we are only interested in the flag value, so
       * we process the two types accordingly.
       */
      if (json.isJsonObject()) {
        JsonElement flagValueElement = json.getAsJsonObject().get("flagValue");
        result = flagScheme.getFlag(flagValueElement.getAsInt());
      } else {
        result = flagScheme.getFlag(json.getAsInt());
      }
    } catch (FlagException e) {
      throw new JsonParseException("Invalid flag value");
    }

    return result;

  }
}
