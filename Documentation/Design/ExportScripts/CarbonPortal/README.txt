The markdown document is set up to be displayed in a web browser using the
Github markdown parser (so it can be viewed directly in Github).

Mermaid diagrams are precompiled as PNGs for ease of display, using the
following mermaid-cli command:

mmdc -w 600 .i x.mermaid -o x.png
